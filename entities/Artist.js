class Artist {
  constructor(id, name, profile, background, albums, songs) {
    this.id = id;
    this.name = name;
    this.profile = profile;
    this.background = background;
    this.albums = albums;
    this.songs = songs;    
  }
}